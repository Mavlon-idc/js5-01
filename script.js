const profile = {}
for(i = 1; i <= 10; i++) {
    console.log('Пользователь ' + i);
    console.log(`Имя ${profile.name = prompt('Введите имя')}`);
    console.log(`Возраст ${profile.age = +prompt('Введите имя')}`);
}